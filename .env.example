# ===========================================
# Roadmap-Nextjs-Backend Environment Variables
# ===========================================
# Copy this file to .env.development or .env.production
# and fill in the values.

# Database (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/roadmap_db?schema=public"

# Authentication (better-auth)
BETTER_AUTH_SECRET="your-secret-key-min-32-characters-long" # You may use `openssl rand -base64 32`
BETTER_AUTH_URL="http://localhost:3000"

# API Configuration
API_KEY_HEADER=x-api-key

# Link on dashboard to core product. Use "#" to disable the link.
REDIRECT_URL="https://your-main-app.com"

# Admin user configuration - Used by `npm run setup:admin` to create the initial admin user. If not provided, defaults are used.
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD=secure123
ADMIN_NAME="Admin User"